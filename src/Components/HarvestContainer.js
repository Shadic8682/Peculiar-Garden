import React from "react";
import { useNavigate, useLocation } from "react-router-dom";

const HarvestContainer = ({harvestedCrops}) => {
    const location = useLocation()

    return(
    <div>
    </div>
    )
}

export default HarvestContainer